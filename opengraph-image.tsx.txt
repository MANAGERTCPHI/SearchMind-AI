import { ImageResponse } from "next/og";

export const runtime = "edge";

export const alt =
  "SearchMind AI - AI Powered SEO Keyword Research";

export const size = {
  width: 1200,
  height: 630
};

export const contentType = "image/png";

export default function OpenGraphImage() {
  return new ImageResponse(
    (
      <div
        style={{
          width: "100%",
          height: "100%",
          display: "flex",
          flexDirection: "column",
          alignItems: "center",
          justifyContent: "center",
          background:
            "linear-gradient(135deg, #0F172A, #2563EB, #7C3AED)",
          color: "#ffffff",
          padding: "80px"
        }}
      >
        <div
          style={{
            display: "flex",
            fontSize: 72,
            fontWeight: 800,
            letterSpacing: "-2px"
          }}
        >
          SearchMind AI
        </div>

        <div
          style={{
            display: "flex",
            marginTop: 24,
            fontSize: 34,
            fontWeight: 500,
            textAlign: "center",
            opacity: 0.9
          }}
        >
          Find High-Value Keywords with AI
        </div>

        <div
          style={{
            display: "flex",
            marginTop: 32,
            fontSize: 22,
            opacity: 0.8
          }}
        >
          AI-powered SEO keyword research platform
        </div>
      </div>
    ),
    {
      ...size
    }
  );
}